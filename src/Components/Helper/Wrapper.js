const Wrapper=(props) => {
    return props.children
}
export default Wrapper
//children props holds all the prperties you are passing
//between the opening n closing tag of your custom component

//now lets go to adduser.js n import there n use as wrapper 